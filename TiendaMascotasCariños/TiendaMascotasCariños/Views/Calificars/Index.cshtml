@model IEnumerable<TiendaMascotasCariños.Models.Calificar>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Adoptar.AdoptarID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comprar.MedioPago)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.calificacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comentario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Adoptar.Mascota.HistorialMascota)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comprar.MedioPago)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.calificacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comentario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CalificarID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CalificarID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CalificarID })
        </td>
    </tr>
}

</table>
